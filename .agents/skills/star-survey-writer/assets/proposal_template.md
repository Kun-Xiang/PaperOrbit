---
type: survey_proposal
slug: <slug>                 # = metds/survey/<slug>/
topic: "<the field, one line>"
reader: <newcomer / practitioner / expert-refresh>
language: <en / zh>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
stage:
  proposal: in_progress      # pending / in_progress / done
  scan: pending
  select: pending
  notes: pending
  outline: pending
  draft: pending
  refine: pending
---

# Survey Proposal — <topic>

## 1. Probe summary

<!-- What the 2–3 exploratory queries showed: the field's apparent shape, activity
     level, and whether prior surveys exist (name them). -->

## 2. Keyword sets

<!-- 3–6 query families, each with its variants — the synonyms the field actually
     uses, benchmark names, "X for Y" phrasings. -->

## 3. Apparent sub-directions

<!-- Each with one example paper from the probe. These seed the outline's coverage
     check. -->

## 4. Selection criteria

<!-- Venue tier list, year window, and the mandatory rows (recent surveys, seminal
     works, frontier representatives) made concrete for this field. -->

## 5. Target scale

<!-- Breadth target (≥100, or the honest smaller number for a narrow field) and pool
     size (15–25). -->

## 6. Deep-pool coverage checklist

<!-- Filled during Step 4: one row per confirmed pool paper — id, note status
     (routed / note-written), citekey once it exists. No note, no characterization. -->

| Paper | Routed to refs-reviewer | Note | Citekey |
|---|---|---|---|

## 7. Decisions

<!-- Dated one-liners: proposal approval, pool confirmation, outline approval,
     reopened stages with reasons. Chat decisions land here — chats end. -->
